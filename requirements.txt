streamlit>=1.44
pandas>=2.0
openpyxl>=3.1
boto3>=1.34
rapidfuzz>=3.8.0
twilio>=9.0.0
# Apple sign-in is the one provider that issues no static client secret: it
# wants a short-lived ES256 JWT, and the crypto extra is what signs it.
pyjwt[crypto]>=2.8
requests>=2.31
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0.0
